---
driver:
  name: podman
platforms:
  - name: instance
    image: geerlingguy/docker-debian12-ansible:latest
    pre_build_image: true
provisioner:
  name: ansible
  log: true
  env:
    ANSIBLE_ROLES_PATH: ../../roles
verifier:
  name: ansible
scenario:
  name: coredns
